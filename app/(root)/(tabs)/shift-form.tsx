import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const ShiftForm = () => {
  return (
    <SafeAreaView>
      <Text>Shift Form</Text>
    </SafeAreaView>
  );
};
export default ShiftForm;
